{
  "name": "castle-crasher",
  "version": "1.0.0",
  "type": "module",
  "description": "A 3D browser-based medieval castle destruction game",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npx prisma generate",
    "preview": "vite preview",
    "postinstall": "prisma generate",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio",
    "api": "node api/server.js",
    "dev:all": "npm run dev & npm run api"
  },
  "dependencies": {
    "@prisma/client": "^6.18.0",
    "cannon-es": "^0.20.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "prisma": "^6.18.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
